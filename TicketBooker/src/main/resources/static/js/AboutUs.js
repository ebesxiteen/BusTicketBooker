tailwind.config = {
    theme: {
        extend: {
            colors: {
                primary: '#10b981',   /* Màu chính (Emerald-500) - Dùng cho Nút, Header */
                secondary: '#047857', /* Màu phụ đậm (Emerald-700) - Dùng cho Footer, Text đậm */
                accent: '#d1fae5',    /* Màu nhấn nhạt (Emerald-100) - Dùng cho nền nhẹ, badge */
            }
        }
    }
}